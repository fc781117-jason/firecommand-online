# FireCommand v28 驗證紀錄

日期：2026-09-07。驗證的是交付包中的原始碼。本次未連到正式站完成登入或發布。

## 已執行

命令：`node --test tests/*.test.cjs`

結果：**49項通過、0失敗**。

| 範圍 | 項數 | 證據與實際涵蓋 |
| --- | ---: | --- |
| 快速回報解析 | 16 | 無位置報到、別字、相似候選、絕對／增減數、分組、移位、水線去重／改接、不明句子阻擋、預覽衝突 |
| 快速回報與應用程式整合 | 12 | 實戰／練習共用、人工核取、連續回報、本機寫入失敗、模擬遠端交易、同命令重送、舊資料衝突、跨案件、復原、摘要、遲到語音與自動預覽 |
| 登入 | 5 | LOCAL／SESSION／NONE降級、Google credential交給Firebase、移除redirect呼叫、公開設定端點 |
| 既有功能整合 | 5 | AI／真人教官、受測角色、確認人車轉圖及旋轉、SDS核對 |
| v27狀態機與解析回歸 | 8 | 時鐘、暫停、逾時、事件去重、提前結束及原解析器驗證 |
| 既有AI API | 3 | 認證／帳號狀態、模擬模型回傳結構、錯誤格式拒絕 |

遠端交易測試使用記憶體Firestore替身，檢查所有讀取在寫入前、無部分提交、原版本比對及同ID去重；**不是Firebase Emulator或正式Firestore規則測試**。語音測試使用SpeechRecognition替身，不是實際麥克風。

另已執行：

- 11個JavaScript檔案的 `node --check`。
- HTML靜態檢查：367個ID，無重複ID；本機script／stylesheet引用均存在；新快速回報label目標存在。
- 人員採用原 `crews`，圖面、清冊、統計及摘要沒有另建平行人數來源。
- `intakeEvents`在Firestore泛用寫入分支明確排除，專屬規則只允許建立與讀取，拒絕更新、刪除；尚未以規則模擬器執行。
- 新Google client ID端點只回傳公開識別值；程式不記錄登入ID token。沒有新增client secret到前端。
- 最終ZIP完整性與MANIFEST_SHA256.json雜湊核對。

## 正式環境必做，尚未執行

| 驗收 | 操作與預期 |
| --- | --- |
| Google登入設定 | 按LOGIN_v28.md設定origin與GOOGLE_WEB_CLIENT_ID，確認正式首頁出現Google官方按鈕，登入後仍是原帳號與案件 |
| 重現舊錯誤 | iPhone原失敗瀏覽器、Safari、App內建瀏覽器分別測試；關閉舊firebaseapp.com頁後，從正式首頁重新登入 |
| 長期回訪 | 隔日重開／還原瀏覽器分頁；無長期儲存時仍能重新登入，不宣稱案件遺失 |
| 語音 | 麥克風允許、拒絕、沒聽清楚、中文車號與分隊口音；確認結果符合原語意 |
| 兩帳號協作 | A預覽4→6人後B先改5人；A送出應被拒絕，重新辨識顯示5→6 |
| 新增衝突 | 兩人同時為同分隊建立語音報到；不重複建立；全部使用v28 |
| 版面與人體工學 | 手機390×844、橫式844×390、桌面1440×900；檢查鍵盤、瀏海安全區、確認按鈕、捲動與戴手套操作 |
| 部署連動 | 原人數4→6後清冊／圖面／概要一致；拖車、改接、旋轉仍保留水線ID |
| 斷線與重送 | 確認時斷網不顯示成功，草稿保留；同次重送不再增員 |
| 權限規則 | 觀察員不能寫；非案件範圍帳號不可讀；審計紀錄不可覆寫；結案不能快速登錄 |

沒有瀏覽器截圖、真機觸控、現場噪音下語音辨識或真實Google OAuth回呼驗證。本版UI為已完成的響應式程式設計，不宣稱已在所有裝置完美適應。
