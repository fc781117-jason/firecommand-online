# FireCommand v26 更新與驗收說明

版本：v26

基準：v25 正式線上版

更新日期：2026-09-04

## 需求對照

| 項目 | v26 實作 | 驗收方式 |
|---|---|---|
| 建物資料自動帶入 | 建物用途、構造、樓層、起火樓層由開案資料帶入「建」SOP | 新建案件後開啟「流程 SOP → 建」確認欄位 |
| 火煙資料自動帶入 | 初報火煙、觀察面向、煙量與起火樓層帶入「火」SOP | 新建案件後開啟「流程 SOP → 火」確認欄位 |
| 關係人／危險物提示 | 說明縮短，選擇已找到／有危險物後才展開資料欄 | 點選「人」階段的關係人與危險物卡片 |
| 文字先行部署 | 部署頁新增文字紀錄；同步至 SOP、概要、報告與繪圖區上方 | 不畫圖先輸入文字，儲存後切換各頁確認 |
| 圖文衝突防呆 | 依圖面中的單位、面向、任務、車輛、水線等關鍵資料比對文字 | 輸入與圖面不同的單位／面向，按一致性檢查 |
| 以圖面為主 | 確認後清除舊文字，保存圖面摘要與確認紀錄 | 在衝突視窗選「我已經確認，以圖面為主」 |
| AI 部署摘要 | 依結構化圖面資料生成續報文字；失敗時用本機摘要 | 建立部署圖後按「AI 依圖面整理部署摘要」 |
| 破門雙時間 | 分為準備破門與破門完成，分別寫入續報／確認資料 | 填兩個時間並儲存到場回報 |
| 戰情兩列收合 | 火勢與傷患者分成兩列，預設收合、互斥展開、送出後收合 | 進入戰情頁並逐一點選兩列 |
| Firebase 登入復原 | 改用 popup、LOCAL persistence、App 內復原提示 | 登出後重新 Google 登入；模擬重新載入確認登入保留 |
| 練習模式 | 手動／AI／檔案建案、房號加入、角色、AI 補位、動態情境 | 首頁切換練習模式，建立房間後由另一帳號加入 |
| 實戰／練習分流 | 首頁兩種模式各自列出案件 | 兩種案件都建立後切換首頁模式 |

## 圖文一致性判定範圍

系統比對的不是任意圖片像素，而是 App 圖面已登錄的結構化項目，包括：

- 人員單位、帶隊官、面向與任務。
- 車輛名稱、單位與任務。
- 水線名稱、來源、終點與任務。
- 危害標示。
- 樓層作業與建物平面圖標示。

這種方式比直接辨識截圖可靠，也能保留可追溯資料。文字與關鍵項目差異過大時會顯示「圖面與紀錄不符」；最終仍由現場使用者確認。

## 登入錯誤根因與處置

`Unable to process request due to missing initial state` 是 Firebase OAuth 回跳頁找不到先前儲存在 `sessionStorage` 的登入初始狀態。常見條件是 iOS／內嵌瀏覽器的跨網站儲存分割、舊登入分頁隔日重開，或瀏覽器清除暫存。案件存在 Firestore，與這個登入暫存不是同一資料，因此按上一頁仍能回到已登入的案件列表。

v26 的新登入改用 popup，不再由 App 啟動 redirect；同時設定 LOCAL persistence。若使用者仍停在舊 Firebase 回跳頁，應按上一頁或關閉該頁，回正式 App 網址重新登入。

## 發布前驗收

- [ ] `node --check assets/app.js`
- [ ] `node --check api/ai-advice.js`
- [ ] HTML 無重複 ID、標籤閉合正確
- [ ] 開案建物／火煙資料可帶入 SOP
- [ ] 破門兩個時間可儲存並出現在續報稿
- [ ] 戰情兩列預設收合且互斥
- [ ] 部署文字可在繪圖時固定顯示
- [ ] 圖文衝突確認可清除文字並改以圖面摘要
- [ ] 手動建立練習房、房號加入及 AI 補位正常
- [ ] AI API 未設定時仍能用本機演練腳本與部署摘要
- [ ] 重新發布 `firebase/firestore.rules`
- [ ] Vercel Production 環境變數與 Firebase Authorized domains 正常

## 相容性與備份

- 沿用既有 `cases` 與原有子集合，不需搬移實戰案件。
- 新增欄位均採相容式讀取；舊案件可以直接開啟。
- 練習資料使用 `mode: "practice"` 與 `players`、`simulationEvents` 子集合。
- 更新 Production 前，建議保留 v25 部署版本及 Firebase 備份，以便快速回復。
