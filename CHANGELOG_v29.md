# FireCommand v29 更新說明

基線：v28。交付日期：2026-09-07。程式包更新，尚未部署正式站。

## 本次新增與修正

- 新增 `intake_parse` AI 端點，使用結構化輸出理解完整消防回報。將人員、車輛、水線、指揮權、第一面、支援與情資分開處理。
- OpenAI Responses 與 Google Gemini 共用資料契約；主要服務、另一服務與備用模型有界切換，顯示實際來源。支援未配置、429、逾時、格式錯誤及截斷結果處理。
- 中文、全形、阿拉伯數字與逐字車號正規化；支援一次多隊報到、同音分隊名稱、總數修正及增減。
- 原本只能閱讀的差異區改為可直接編輯欄位，含分隊與大隊、編組對象、人數、增減方式、車號、面向、水線終點、支援與紀錄內容。
- 每項可勾選／延後／移除；不明項目有獨立核對勾選。修改後清除舊確認狀態，避免未核對變更被直接儲存。
- 增加手動新增項目，AI 漏項時不必返回原句反覆嘗試。
- 已確認項目可先送出，不明項目保留至回報歷史繼續處理；沒有資源變更的情資與 SOP 回報也能登錄。
- 草稿與確認交易共用於實戰／練習；保持現有 ID、未提及欄位與手動部署。人車水線、SOP 與紀錄原子提交，重送不重複，衝突時重新核對。
- 沒有來源車號的單位水線可建立「供水起點待確認」示意線。補來源時沿用原線；手動畫面也可選來源車輛。多組既有線混雜時請逐線確認，不猜測合併。
- 將快速回報整合到 SOP 初期部署，移除案件首頁大按鈕及人員頁重複按鈕；輸入、AI 設定、歷史、明細均可收合。
- 移除 App 自建語音收音，包括練習回應按鈕。僅提示使用手機鍵盤語音轉文字，不向瀏覽器要求錄音權限。
- 調整窄螢幕單欄、桌機多欄、橫式低高度與觸控欄位尺寸；核對區直接使用頁面捲動，避免全螢幕對話框與頁底導覽互相遮擋。
- 原文上限調整為 6,000 字，Firestore Rules 同步更新。

## 保留功能

v28 的直接 Google 憑證登入、Firebase 儲存降級、部署旋轉、人車手動調整、AI 教官、練習計時與暫停、SDS 原文核對均保留。原有 AI 報告、建議、情境與 SDS 摘錄也使用共用服務備援路由。

## 驗證狀態

77 項 Node 自動測試通過；HTML 標籤、370 個 ID、前端依賴與 13 個 JavaScript 檔語法檢查通過。模型與 Firebase 均採模擬回應，未實測付費 API、正式站、iPhone 或瀏覽器版面。

部署需更新整包、AI 環境變數及 Firestore Rules。設定見 `AI_SETUP_v29.html`；真機與多人驗收見 `VALIDATION_v29.md`。
