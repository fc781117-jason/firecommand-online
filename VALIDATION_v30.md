# FireCommand v30｜驗證及現場驗收

程式完成日期：2026-09-10。版本以包內 index.html 的 v30 與 `/api/maps-config` 的 version=30 為準。

## 已完成的自動驗證

執行 `node --test tests/*.test.cjs`：**96 項通過、0 失敗**。輸出：`tests/test-results-v30.txt`。

| 範圍 | 已檢驗內容 |
|---|---|
| 截圖缺陷 | 選對水線終點仍被舊面向擋住；無法逐字對照原句造成多重核對；單項錯誤阻擋整頁 |
| 辨識與動作 | 中文數字、么／幺／拐／洞、111與1116區別、正面語意、已進入與預定區分 |
| 逐筆流程 | 修改事件更新草稿、點單筆按鈕儲存、下一筆採最新數量、同筆重點不重複增員 |
| 水線與車組 | 來源依賴車輛同筆登錄、串接拓樸、整組移位、復原、兩線分開且端點不變 |
| 同步 | 既有編組ID、人數修正與總數、SOP情資、回報摘要、舊預覽遇到新修改時拒絕覆蓋 |
| 權限與服務 | 實戰／練習共同入口、觀察員限制、API驗證模擬、429與逾時備援、推理參數 |
| 畫面結構 | 三張主卡，其餘隊列收合；文字與欄位預設收合；入口保留在初期部署 |

既有測試中，與新版操作已不符的 3 個 UI 斷言已更新為單筆按鈕事件與 v30 資源路徑；保留其他核心、登入及 API 回歸項目。新測試針對本次缺陷及連動風險，不以新增測試數量代表真實操作效果。

## 仍需實機完成的驗收

| 步驟 | 操作 | 應看到的結果 |
|---|---|---|
| 1 | 固定正式網址開啟 | v30；首頁沒有獨立語音捷徑；登入協助是新教學 |
| 2 | Safari 從首頁登入，登出再登入 | 使用原帳號，保留既有權限與案件；不回到舊錯誤頁 |
| 3 | 初期部署輸入「淡水七人從正面已進入；竹圍六人到場」 | 人員卡各自確認；淡水第一面作業中；竹圍沒有位置時待部署 |
| 4 | 故意讓另一筆水線缺終點，再確認人員 | 人員先完成；缺欄位只展開該水線修改 |
| 5 | 輸入「淡水111正面出兩條水線；淡水16供水給淡水111」 | 111車在第一面，兩條進攻線、一條供水線；無重複車輛 |
| 6 | 拖動車組，再復原 | 相連車一同移動，回報稿位置更新；復原整組回原處 |
| 7 | 下一次輸入「淡水新增兩人」，確認一次後重點同筆 | 淡水由7變9，同筆不會再變11 |
| 8 | 有兩個同分隊編組時修正人數 | 只對明確選取的編組更新 |
| 9 | 兩個帳號同時修改同一隊 | 舊草稿不能覆蓋剛更新資料；重新核對後才提交 |
| 10 | OpenAI、Gemini 各自辨識同一組口述 | 核對實際服務、模型、等待時間、誤認與漏項；不能只看「已設定」 |
| 11 | 直向手機、較大文字、日光環境 | 按鈕可點、沒有左右溢出、底部導航不遮住操作；尚需實測 |

本次未進行瀏覽器視覺QA、真實 API 呼叫、Google Maps 載入或 Firebase 規則模擬器驗證。自動驗證依現有本機及模擬服務測試架構執行。語音使用手機鍵盤，辨識結果與裝置、收音環境及輸入方式有關。

## 明確限制

- 模型輸出固定結構可降低格式錯誤，但不能保證內容皆正確；所有動作仍需人工確認。
- 本機整理是規則處理，完整跨句語意仍以 AI 草稿為主。未支援資訊可作情資保留及人工補登。
- 不知道車號時保留供水起點待確認示意線；無法由口述推得樓層內精確位置。
- 「A接B」只確定連結，未指明流向就不宣稱水由A供往B。
- 供水群組的地理移位不自動改接建物水線終點；改接需另外回報或編輯水線。
- 本次沒有直接將訊息發送至北海或其他外部通訊；系統產出可核對的回報稿，沿用原本回報方式。
- 尚未改成完整離線同步系統；連線失敗不顯示成已成功寫入。權限與帳號依既有 Firebase 規則。
- 不會僅因不確定就自動呼叫更昂貴模型；本版仍以服務／格式失敗備援。
