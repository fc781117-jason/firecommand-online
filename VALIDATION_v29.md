# FireCommand v29 驗證紀錄

2026-09-07。以下區分已完成的程式驗證與尚待部署的實際驗收。

## 已完成

執行：`node --test tests/*.test.cjs`。結果：**77 通過，0 失敗，0 跳過**。

| 驗證範圍 | 結果與實際限制 |
|---|---|
| 使用者截圖回報 | 本機解析可讀出淡水7、竹圍6、三芝3，合計16；指揮／面向／支援／情資分開；不把石台猜十台 |
| 數字與名稱 | 中文、全形、阿拉伯、壹拾陸等大寫數字、六一等逐字車號；三芝仍是單位名 |
| 重複與連續回報 | 相同絕對值不重複增加；增減與總數修正分開；未提及面向任務保留 |
| 編輯及確認 | 實際綁定的 `oninput` 會更新草稿；修改後需重新確認；取消選入不明項目後可提交清楚項目 |
| 純情資／SOP | 沒有人車變更也能提交；復原會還原相應 SOP 差異；後續變更阻止覆蓋 |
| 水線 | 無車號不建立假車；示意線補來源沿用 ID；來源車移動不改終點；減線、改接保持正確引用 |
| 多組同分隊 | 可選編組 ID；未選時不任意覆蓋全隊 |
| AI 呼叫契約 | intake API 會呼叫模型；回傳結構化草稿與來源；所有付費模型呼叫前驗證帳號 |
| 雙供應商備援 | 模擬 OpenAI 429 → Gemini；兩家失敗再用備用模型；最多3次；手動停用備援只呼叫指定一家 |
| 輸出與服務錯誤 | 壞 JSON、逾時及截斷結果不當成功；內容拒絕不跨家重試；缺金鑰不外呼 |
| 附件與金鑰 | Gemini 備援保留 SDS／情境來源附件；對外回應不包含 API key |
| 案件與文字切換 | 晚到的 AI 結果不寫入新案件或已修改原文；取消後可重新操作 |
| Firebase 交易 | 使用模擬交易驗證原子提交、重送、防並行覆蓋、SOP 衝突及拒絕部分寫入；非正式 Firebase 連線測試 |
| 原有功能回歸 | 練習角色權限、計時、暫停、事件防重複、建物旋轉、SDS 來源確認與 v28 登入邏輯 |

靜態檢查：HTML 標籤正確閉合；370 個 ID 無重複；引用的本地 scripts／CSS 均存在；13 個前端、後端 JavaScript 檔通過 `node --check`。確認自建 SpeechRecognition／getUserMedia 與舊麥克風按鈕已移除。

測試是函式、事件處理器及模擬交易層級；**不是瀏覽器自動化點擊、渲染截圖、真實語音、真實模型或 Google Maps 服務測試**。

## 部署後必做

1. 確認全包發布成功、版本 v29、AI 設定頁可開啟、Firestore Rules 已更新。
2. 在 iPhone Safari 與電腦以正式 App 首頁登入；測試關閉後重開、隔日重開及 App 內瀏覽器轉外部瀏覽器。核對公開 Google OAuth client ID 與合法網域設定。
3. 在練習案件使用實際 OpenAI key，關閉備援，測試三隊 16 人報到。再以 Gemini 單獨測試同一段回報。確認格式、耗時及真實辨識品質，不能只看「已設定」字樣。
4. 將淡水人數改為中文「九」，直接在卡片核對；更新後三隊合計18。第二次回報「竹圍新增兩人」，合計20，原有隊伍 ID 不變。
5. 輸入「需要石台水庫車支援」，確認不會捏造數量或已到場車輛。直接修正需求數、單項勾選與總確認後，檢查 SOP 支援欄。
6. 未說車號的水線應為橘色虛線並標示來源待確認；補車號後核對同一條線。拖曳車輛、旋轉建物、手動改接水線，確認人車與拓樸正確。
7. 兩個有權限帳號同時進入練習案件及測試實戰案件，觀察人員、圖面、SOP、摘要與紀錄更新；對同一編組同時修改應要求重新核對。
8. 在 iPhone 直式／橫式與桌機確認：初期部署不自動展開所有細節、鍵盤不擋送出、長單位名稱不溢出、每個編輯欄位可點、勾選與取消有效、頁底導覽不遮內容。
9. 在測試部署模擬一家的無效 key／無權限模型，確認依規劃備援；測試後恢復有效設定並重新部署。請勿故意破壞正式站金鑰作驗收。
10. 檢查原有 Google 地圖、報告、SDS 與練習主持流程沒有回歸。

## 尚未宣稱完成

未部署正式站、未更改 Google Console／Vercel 環境變數、未發布 Firebase 規則、未使用真實 AI API、未做 iPhone 真機及瀏覽器視覺驗收。模型版本／帳務實際可用性與跨帳號同步仍需正式環境確認。
